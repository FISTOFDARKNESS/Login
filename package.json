{
  "name": "product-catalog",
  "version": "1.0.0",
  "description": "Product catalog with reviews",
  "scripts": {
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.8.0"
  }
}
